


<div class="p-4 md:w-1/3 sm:mb-0 mb-6">
    
    <h2 class="text-xl font-medium title-font text-white mt-5">{{ job.title }}</h2>
    <!-- We need to truncate the description and add ... at the end -->
    <p class="text-base leading-relaxed mt-2">{{ job.description|slice(0, 100) }}...</p>
    <a href = "{{ path('app_job_show',{'id': job.id}) }}"" class="text-indigo-400 inline-flex items-center mt-3"
      >Learn More
      <svg
        fill="none"
        stroke="currentColor"
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        class="w-4 h-4 ml-2"
        viewBox="0 0 24 24"
      >
        <path d="M5 12h14M12 5l7 7-7 7"></path>
      </svg>
    </a>
  </div>
  